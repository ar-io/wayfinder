---
"@ar.io/wayfinder-core": minor
---

Add `DataRetrievalStrategy` interface for fetching data from AR.IO gateways. Implement `ContiguousDataRetrievalStrategy` (default) and `ChunkDataRetrievalStrategy`.
